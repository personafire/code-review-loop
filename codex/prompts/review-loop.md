Run the code review loop defined in
`plugins/review-loop/skills/review-loop/SKILL.md` and its `references/` files.
Apply it to the current changes; if an argument is provided, use it as the
review-target override. Follow the loop through to the final report.
