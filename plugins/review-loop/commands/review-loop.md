---
description: Run the multi-agent code review loop on the current changes
---

Invoke the `review-loop` skill and follow it to completion on the user's current
changes. If the user passed an argument (a PR, branch, `staged`, or paths), use
it as the review target override.
